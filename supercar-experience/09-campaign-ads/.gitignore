# Footage never goes in git - plates are rebuilt from the raw with make_plate.py
*/source/*.mp4
*/source/*.mov
# Rendered spots are delivered as downloads, not committed; stills and posters are kept
*/exports/*.mp4
.frames/
.measure/
__pycache__/
.hosting/

# Built by build_deliverables.py: hardlink trees of the gitignored renders,
# plus their zips. Rebuild with `python3 build_deliverables.py`.
_deliverables/
