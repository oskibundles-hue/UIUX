{
  "mcpServers": {
    "nano-banana": {
      "command": "bash",
      "args": [
        "-c",
        "cd tools/nano-banana-mcp && { [ -d node_modules ] || npm install --omit=dev --no-audit --no-fund 1>&2; } && exec node dist/index.js"
      ],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}"
      }
    }
  }
}
