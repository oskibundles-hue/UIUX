# Per-clip properties that are facts about the FOOTAGE, not choices about the
# edit. Anything here has to survive being forgotten: clip 12 was shot with the
# camera inverted, was fixed once by passing --rotate 180 by hand, and then came
# back upside down the moment it was re-cut through a script that did not know
# to pass it. A property of the clip belongs with the clip.
#
# NN  rotate
12    180
